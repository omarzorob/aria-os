# ============================================================
# Aria OS — Environment Variables
# Copy to .env and fill in your values
# Never commit .env to version control!
# ============================================================

# ------ Web Search ------
# Brave Search API key (https://brave.com/search/api/)
BRAVE_API_KEY=your_brave_api_key_here

# ------ Email (SMTP + IMAP) ------
# Gmail example: smtp.gmail.com / imap.gmail.com
# Use App Passwords for Gmail (not your regular password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here

# ------ Maps / Navigation ------
# Google Maps API key (optional — enables Directions API + Places API)
# Without it, maps tool opens the Maps app on-device
GOOGLE_MAPS_API_KEY=your_google_maps_key_here

# ------ Music ------
# Preferred music app: "spotify" or "youtube_music"
MUSIC_APP=spotify

# Optional: Spotify Web API (for search without opening app)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# ------ Food / Grocery Delivery ------
UBEREATS_DELIVERY_ADDRESS=Frankfort, IL 60423
UBEREATS_EMAIL=your@email.com

INSTACART_DELIVERY_ADDRESS=Frankfort, IL 60423
INSTACART_EMAIL=your@email.com

# ------ Aria Agent Server ------
ARIA_SERVER_HOST=0.0.0.0
ARIA_SERVER_PORT=8765
ARIA_DEBUG=false

# ------ LLM Backend ------
# Aria uses the configured LLM provider
OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here

# ------ ADB ------
# Path to adb binary (default: "adb" from PATH)
ADB_PATH=adb
# Device serial (for multi-device setups)
# ADB_DEVICE_SERIAL=ABC123DEF456

# ------ APK Signing (build_apk.sh) ------
KEYSTORE_PATH=/path/to/aria-keystore.jks
KEYSTORE_PASS=your_keystore_password
KEY_ALIAS=aria
KEY_PASS=your_key_password
